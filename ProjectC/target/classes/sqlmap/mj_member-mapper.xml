<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="member.mapper">

	<!-- 회원정보 불러오기 -->
	<select id="detail" resultType="mj_member.MemberVO">
	select *
	from member
	where email = #{email}
	</select>


	<!-- 회원정보 수정 -->
	<update id="member_update">
		update member set
		nickname=#{nickname}, password=#{password}, address=#{address},	filepath=#{filepath}
		where email=#{email}
	
	
	</update>


	<!-- 로그인 (id, pw 일치 여부 조회) -->
	<select id="member_login" resultType="mj_member.MemberVO">
		select * from member
		where email = #{email}
		and password = #{password}	
	</select>

	<!-- social_email 존재 여부 조회 (소셜 로그인 이력 조회) -->
	<select id="social_email" resultType="integer">
		select count(*) from member
		where ${social_type} = #{social_email}
		<!-- ${social_type} 내 들어있는 동적 데이터 (즉, naver, kakao) 를 불어옴 -->
	</select>
	
	<update id="social_update">
		update member set name = #{name}, gender = #{gender}
		where ${social_type} = #{social_email}
	</update>
	

	<insert id="social_insert">
		insert into member (email, name, ${social_type})
		values ( #{email}, #{name}, #{social_email} )
	</insert>

	<select id="id_check" resultType="integer">
		select count(*) from member
		where id = #{email}
	</select>

	<insert id="join">
		insert into member (nickname, password, address, email, idnumber, name)
		values ( #{nickname}, #{password}, #{address}, #{email}, #{idnumber}, #{name})	
	</insert>
	
	
	
	

</mapper>